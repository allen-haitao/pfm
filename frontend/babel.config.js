module.exports = {
    presets: [
        '@babel/preset-env',
        '@babel/preset-react',  // Add this if you're using React
    ],
    plugins: [
        '@babel/plugin-proposal-class-properties',
    ],
};